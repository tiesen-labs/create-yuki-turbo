generator client {
  provider        = "prisma-client"
  previewFeatures = ["prismaSchemaFolder", "driverAdapters"]
  output          = "../generated"
  moduleFormat    = "esm"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
