import { handlers } from '@yuki/api'

// export const runtime = 'edge'

export {
  handlers as GET,
  handlers as POST,
  handlers as PUT,
  handlers as PATCH,
  handlers as DELETE,
  handlers as OPTIONS,
}
