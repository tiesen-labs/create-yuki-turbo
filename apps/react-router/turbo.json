{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".react-router/**", "build/**"]
    },
    "dev": { "persistent": true }
  }
}
